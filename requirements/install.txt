#use specific pip version for dependencies to work nicely
pip==25.2
pip-tools==7.5.1

#core requirements
requests

#install requirements. 
invoke==2.2.1
wheel
psutil==6.1.1 # look at the processes for games

galaxy.plugin.api==0.70